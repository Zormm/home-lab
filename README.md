# home-lab

A collection of utilities for home lab management.

## Input Validation

The `validate_input.py` script provides basic input validation functionality to ensure configuration values are valid.

### Usage

```bash
python3 validate_input.py
```

This will run the built-in test cases to verify the validation logic is working correctly.